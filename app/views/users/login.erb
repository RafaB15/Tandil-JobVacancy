<h1>Login</h1>
<% form_for(@user, '/users/login') do |f| %>
	<%= f.label :email %>
 	<%= f.text_field :email %>
  <%= f.label :password %>
  <%= f.password_field :password %>
  <br/>
  <%= f.submit "Login", :class => "btn btn-primary" %>
<% end %>